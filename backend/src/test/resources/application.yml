server:
  port: 8082

spring:
  application:
    name: backend-test
  datasource:
    url: jdbc:h2:mem:resumerank_test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDER=HIGH
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true
    show-sql: false
  flyway:
    enabled: false

jwt:
  secret: dGhpcy1pcy1hLXN1cGVyLXNlY3JldC1rZXktdGhhdC1pcy1hdC1sZWFzdC0yNTYtYml0cy1sb25nLWZvci1obWFjLWZvci10ZXN0aW5nLXVzZQ==

cloudinary:
  cloud-name: mock-cloud-name
  api-key: mock-api-key
  api-secret: mock-api-secret

resend:
  api-key: mock-resend-key
  from-address: onboarding@resend.dev

app:
  frontend-url: http://localhost:3000
